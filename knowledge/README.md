# knowledge/

Reference documents for the akm CLI, stash structure, and adjacent concepts.
Knowledge assets are read-only context — load them with `akm show
knowledge:<ref>` when an agent needs background, not when it needs to act.

Browse the full set with:

```bash
akm search --type knowledge
```
