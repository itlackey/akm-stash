# knowledge/

Reference documents for the akm CLI, stash structure, and adjacent concepts.
Knowledge assets are read-only context — load them with the ref of the
document you want, for example:

```bash
akm show knowledge:akm-overview
```

Use them when an agent needs background, not when it needs to act.

Browse the full set with:

```bash
akm search --type knowledge
```
